```js
const slidingwindow = (str, target) => {
  let need = {}, window = 0;
  for(let i = 0; i < t.length; i++) {
    let item = t[i];
    if(!need[item]) {
      need[item] = 1;
    } else {
      let count = need[i];
      need[i] = ++count;
    }
  }

  let left = 0, right = 0;
  let valid = 0;
  while(right < str.length) {
    
  }

}
```